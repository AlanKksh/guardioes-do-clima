services:
  app:
    build: .
    container_name: guardioes-do-clima-ui5
    ports:
      - "8080:8080"
    volumes:
      - .:/app
      - /app/node_modules
    environment:
      CHOKIDAR_USEPOLLING: "true"
